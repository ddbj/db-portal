export type paths = Record<string, never>

export type components = {
  schemas: {
    "DbPortalParseBoolOp-Input": never
    "DbPortalParseBoolOp-Output": never
    DbPortalParseLeafValue: never
    DbPortalParseLeafRange: never
    DbPortalParseFreeText: never
  }
}

export type operations = Record<string, never>
