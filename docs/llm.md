### 2.1 `checker` 7 種は明らかに多い

`§3.3` の優先順 4「機能を絞る」と直接矛盾。以下で十分 MVP の価値が出る:

いや、これ、ぜんぶだいじなんだよね。。。
deadman とか何なの？

### 2.2 `notifier` 5 種も絞れる

mail, file はいらないかも。

### 3.1 `notify_strategy: transition` (default) が `§3.3` 優先順 3「取りこぼし < 過剰通知」と矛盾

これはなんのことを言っている？リマインドは基本いらないよ。

### 3.2 `mitsume run -- cmd` 中のシグナル forward が未定義 (§5.1)

これは大事だと思う。

### 3.3 設定 JSON の validation 仕様が未定義

んー、設定 JSON 自体の詰めは残件だと思っていた。
まず、ソフトウェアの使用イメージをもっと固めたいんだよね。

### 3.4 設定なしモードでの `state_file` 解決戦略が未定義

state.db じたい、必要かどうか迷っている。軽量というコンセプトから外れそうな気がするんだよね。

### 3.5 check id の命名規則が未定義

そもそも、id という概念はいらないのでは？とも思っている。ちょっと相談しよう。

### 3.6 timezone (`tz`) は config なしモードでも効くか

server (実行環境の) の tz をそのまま使おう

### 4.1 数値比較 (`gt` / `lt`) を `expect` に入れるか (§7.1)

これも、監視対象のユースケース分析が必要だと思う。
「そう。全てにおいて、ユースケース分析をしてないかも！！」
