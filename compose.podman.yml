# Override layered on top of compose.yml for podman / podman-compose deployments
# on the NIG infrastructure. Use:
#   podman-compose -f compose.yml -f compose.podman.yml up -d
services:
  app:
    userns_mode: keep-id
    security_opt:
      - label=disable
