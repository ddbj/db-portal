# Block recently-published versions to mitigate short-lived supply-chain
# attacks on npm. Filters lockfile resolution only (npm install / update /
# audit fix); npm ci honors the existing lockfile regardless. Requires
# npm >= 11.10.
min-release-age=7
